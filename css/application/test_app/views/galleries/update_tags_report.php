<h2>Gallery Tools</h2><p><?php echo (count($msg));?> Record processed.</p><ul><?php foreach ($msg as $row){	print ("<li>" . $row . "</li>"); }?>  </ul><hr />
	  <form id="form1" name="form1" method="post" action="<?php echo (WEBROOT)?>galleries/update_tags">	    <h3>Bulk Update Record Tag Data</h3>		<p>Paste data into the field below to update multiple records to the database. Each record should be on a separate line, fields should be separated by the "|" character.</p>        <p><b>Key:</b> Id* | Gallery Section Code* (All Id's should ALREADY exist in the database.)</p>		<hr size="1" noshade />		<textarea name="textarea" cols="80" rows="8" wrap="off"><?php echo $textarea?></textarea>				<br />		<input type="checkbox" id="preview" value="<?php echo $lookup ?>" name="preview" checked="<?php echo $preview ?>" /> <label for="preview">Preview Mode (will not update the database)</label>		<br />		<input type="checkbox" id="lookup" value="<?php echo $lookup ?>" checked="<?php echo $lookup ?>" name="lookup" /> <label for="lookup">Lookup IDs (will return list of URL paths)</label>				<br /><br />        <input type="submit" name="submit" value="Process" class="button"/>	  </form>	  