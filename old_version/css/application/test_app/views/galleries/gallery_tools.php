<h2>Gallery Tools</h2>
	  <form id="form1" name="form1" method="post" action="<?php echo (WEBROOT)?>galleries/update_tags">	    <h3>Bulk Process Tag Data (or look up)</h3>		<p>Paste data into the field below to update multiple records to the database. Each record should be on a separate line, fields should be separated by the "|" character. For <b>LookUp</b> just enter a list of IDs, one per line, and it will look up the records and return a list of URL paths. Be sure to check the box beneath.</p>        <p><b>Key:</b> Id* | Gallery Section Code* (All Id's should ALREADY exist in the database.)</p>		<hr size="1" noshade />		<textarea name="textarea" cols="80" rows="8" wrap="off"></textarea>				<br />		<input type="checkbox" id="preview" value="true" name="preview" /> <label for="preview">Preview Mode (will not update the database)</label>		<br />		<input type="checkbox" id="lookup" value="true" name="lookup" /> <label for="lookup">Lookup IDs (will return list of URL paths)</label>				<br /><br />        <input type="submit" name="submit" value="Process" class="button"/>	  </form>