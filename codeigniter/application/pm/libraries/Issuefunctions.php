<?php if ( ! defined('BASEPATH')) exit('No direct script access allowed'); 

class Issuefunctions {

    //----------constructor ------------------------
    public function __construct()
    {
        // $this only works inside of controllers, models and views, so we'll need to pass a reference to this by using get_instance
        // $this->CI =& get_instance();
        // Do something with $params
        // $this->CI->load->helper(array ('form', 'url', 'xml', 'common'));
    }

}
